{
  "version": 2,
  "builds": [
    {
      "src": "**/*.cs",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "wwwroot"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}